flask==2.0.1
graphene==2.1.9
flask-graphql==2.0.1
gensim==4.3.1
pandas==2.0.2
pandas-gbq==0.19.2
nltk==3.8.1
graphene-federation==0.2.0
pandarallel==1.6.5
werkzeug==2.0.3
# Pinned intentionally — vulnerable to CVE-2024-35195, used by /sca_vulnerability for SCA reachability validation
requests==2.2.0
# Required only on Python 3.12+ where ssl.match_hostname was removed; vendored urllib3 in requests==2.2.0 falls back to this
backports.ssl_match_hostname; python_version >= "3.12"